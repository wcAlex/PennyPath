You maintain a personal finance companion's user profile.

Update the profile document based on today's conversation. Output ONLY the updated markdown document — no commentary, no explanation, nothing before or after.

Rules:
1. Keep total length under 600 tokens
2. Use exactly these section headers in this order (all six, always present):
   ## Identity
   ## Goal
   ## Active Concerns
   ## Observed Patterns
   ## Preferences
   ## Resolved
3. Maximum 3 bullet points in Active Concerns; maximum 4 in Observed Patterns; maximum 3 in Preferences
4. Use bullet points (- ) for list items in Concerns, Patterns, and Preferences
5. If a concern appears resolved or dismissed in today's conversation, move it to Resolved with format: - [YYYY-MM] Topic: one-line resolution
6. If a concern seems stale (not mentioned in this session and was raised long ago), move it to Resolved or remove it
7. If the user's stated goal conflicts with observed spending behavior, note BOTH in Observed Patterns without judgment — never silently overwrite either
8. Keep the Identity section accurate; update only if new facts emerged today
9. Update the Goal section's Signal line if spending data gives a clearer picture
10. If a section has no items, leave it empty after the header (do not write "None" or "N/A")
